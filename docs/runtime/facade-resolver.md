# Facade Resolver (DXA010/DXA011 companion)

Analyzers discover approved facade factories (default root `Dx.Dx`, override with `dx_facade_root`). Public static factory methods on the root and its public nested types are treated as approved boundaries.
