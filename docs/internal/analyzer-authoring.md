# Analyzer Authoring

Each analyzer must declare one stable ID, title, category, message, default severity, enabled-by-default state, and description. Add focused positive, negative, scope, configuration, generated-code, and regression tests. Update generated metadata, the public diagnostic reference, the diagnostic router, changelog, and release notes. Document false-positive strategy and known bypasses. Diagnostic IDs are append-only and must never be repurposed.
