# Development setup

This page routes readers to the environment and tooling needed to work on Dx.Domain.

- Restore tools with `dotnet tool restore`.
- Restore packages with `dotnet restore`.
- Build and test before documentation validation.
- Refer to [local validation](local-validation.md) for the canonical sequence.
