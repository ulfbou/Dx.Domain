# DXA022 — Result vs Throw Discipline
**Intent**: Public (or public‑enough) `Result` methods must not throw domain outcomes.  
**Applies to**: Consumers (`public`/`protected` API).  
**Allows**: Invariants, guard throws, impossible states, rethrow.  
**Remediation**: Return `Result.Failure` with canonical error value.
