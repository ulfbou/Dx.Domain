# DXA080 — Facade Invariant Enforcement Missing
**Intent**: Facade factory methods must enforce invariants (often via `Result`).  
**Remediation**: Enforce invariants and return proper failure values.
