# Build a Construction Boundary

Place validation and creation in a small S1 boundary configured as the facade root. Return `Result<T>` for expected failures, keep domain constructors non-public where practical, and avoid infrastructure behavior in the boundary.

DXA010, DXA011, and DXA080 check common static patterns. Review each diagnostic page for exact alpha severity and limitations.
