# Legacy architecture route

> **Legacy route:** This page is retained for compatibility and is not an authoritative architecture description.

Read the [canonical public architecture](../public/architecture.md), [stability policy](../public/stability.md), and [diagnostic reference](../public/reference/diagnostics/index.md).

Consumers install `Dx.Domain.Analyzers` explicitly during alpha. Standard compiler suppression mechanisms remain technically available; repository policy may separately restrict suppression. Runtime APIs and analyzer behavior are provisional for `0.1.0-alpha`.
